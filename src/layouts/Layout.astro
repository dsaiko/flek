---
interface Props {
  titleCs: string;
  titleEn: string;
  titleDe: string;
  descriptionCs: string;
  descriptionEn: string;
  descriptionDe: string;
}

const { titleCs, titleEn, titleDe, descriptionCs, descriptionEn, descriptionDe } = Astro.props;
const GOATCOUNTER = 'https://saiko-flek.goatcounter.com/count';
---
<!doctype html>
<html lang="cs">
<head>
  <meta charset="UTF-8" />
  <!--
    CSP: statická stránka bez backendu, takže politika žije v <meta> (musí být
    hned za charset, platí až od svého výskytu). Skripty jen z vlastního
    původu + gc.zgo.at (analytika), 'unsafe-inline' kvůli is:inline blokům
    Astra; beacon GoatCounteru chodí jako <img> na *.goatcounter.com.
  -->
  <meta
    http-equiv="Content-Security-Policy"
    content="default-src 'self'; script-src 'self' 'unsafe-inline' https://gc.zgo.at; style-src 'self' 'unsafe-inline'; img-src 'self' data: https://*.goatcounter.com; connect-src 'self' https://*.goatcounter.com; worker-src 'self' blob:; font-src 'self'; object-src 'none'; base-uri 'self'; form-action 'self'"
  />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta name="description" content={descriptionCs} />
  <meta name="theme-color" content="#1e5c38" />
  <link rel="canonical" href={new URL('/', Astro.site)} />
  <script is:inline>
    (function(){var l='cs';try{var q=(location.search.match(/[?&]lang=(cs|en|de)/)||[])[1];
     var st=localStorage.getItem('lang');
     if(q)l=q;else if(st==='en'||st==='de')l=st;}catch(e){}
     window.__lang=l;document.documentElement.classList.add('lang-'+l);})();
  </script>
  <title>{titleCs}</title>
  <style is:global>
    * { box-sizing: border-box; }
    .lang-cs .en, .lang-cs .de,
    .lang-en .cs, .lang-en .de,
    .lang-de .cs, .lang-de .en { display: none; }

    .home-link, .langpill {
      position: fixed; top: 18px; z-index: 30;
      background: rgba(255,255,255,.94); border: 1px solid #e5e7eb;
      box-shadow: 0 2px 10px rgba(17,24,39,.08); backdrop-filter: blur(10px);
    }
    .home-link {
      left: 18px; display: inline-flex; align-items: center; gap: 7px;
      color: #374151; border-radius: 999px; padding: 8px 16px;
      font: 600 14px/1.2 system-ui, sans-serif; text-decoration: none;
    }
    .home-link:hover { background: #f9fafb; }
    .home-link .arrow { transition: transform .12s; }
    .home-link:hover .arrow { transform: translateX(-3px); }

    .langpill { right: 18px; display: flex; gap: 4px; border-radius: 999px; padding: 4px; }
    .langpill button {
      display: flex; align-items: center; justify-content: center; width: 36px; height: 27px;
      border: 0; background: none; border-radius: 999px; cursor: pointer; padding: 0;
      opacity: .45; transition: opacity .12s, background .12s;
    }
    .langpill button:hover { opacity: .8; background: #f3f4f6; }
    .langpill button.on { opacity: 1; background: #eef2ff; }
    .langpill svg { display: block; border-radius: 2px; box-shadow: 0 0 0 1px rgba(0,0,0,.06); }
  </style>
</head>
<body data-title-cs={titleCs} data-title-en={titleEn} data-title-de={titleDe} data-description-cs={descriptionCs} data-description-en={descriptionEn} data-description-de={descriptionDe}>
  <a class="home-link" href="https://www.saiko.cz/"><span class="arrow">←</span><span class="cs">PROJEKTY</span><span class="en">PROJECTS</span><span class="de">PROJEKTE</span></a>

  <div class="langpill" role="group" aria-label="Language / Jazyk">
    <button data-lang="cs" title="Čeština" aria-label="Čeština">
      <svg width="24" height="16" viewBox="0 0 6 4" xmlns="http://www.w3.org/2000/svg">
        <rect width="6" height="2" fill="#fff"/><rect width="6" height="2" y="2" fill="#d7141a"/>
        <path d="M0,0 L3,2 L0,4 Z" fill="#11457e"/>
      </svg>
    </button>
    <button data-lang="de" title="Deutsch" aria-label="Deutsch">
      <svg width="24" height="16" viewBox="0 0 5 3" xmlns="http://www.w3.org/2000/svg">
        <rect width="5" height="3" fill="#FFCE00"/><rect width="5" height="2" fill="#DD0000"/><rect width="5" height="1" fill="#000"/>
      </svg>
    </button>
    <button data-lang="en" title="English" aria-label="English">
      <svg width="24" height="16" viewBox="0 0 60 30" xmlns="http://www.w3.org/2000/svg">
        <clipPath id="gb-a"><rect width="60" height="30"/></clipPath>
        <clipPath id="gb-b"><path d="M30,15 h30 v15 z v15 h-30 z h-30 v-15 z v-15 h30 z"/></clipPath>
        <g clip-path="url(#gb-a)">
          <rect width="60" height="30" fill="#012169"/>
          <path d="M0,0 L60,30 M60,0 L0,30" stroke="#fff" stroke-width="6"/>
          <path d="M0,0 L60,30 M60,0 L0,30" clip-path="url(#gb-b)" stroke="#C8102E" stroke-width="4"/>
          <path d="M30,0 v30 M0,15 h60" stroke="#fff" stroke-width="10"/>
          <path d="M30,0 v30 M0,15 h60" stroke="#C8102E" stroke-width="6"/>
        </g>
      </svg>
    </button>
  </div>

  <slot />

  <script is:inline>
    (function(){
      var d=document.body.dataset;
      var TITLES={cs:d.titleCs,en:d.titleEn,de:d.titleDe},DESCRIPTIONS={cs:d.descriptionCs,en:d.descriptionEn,de:d.descriptionDe};
      function apply(l){
        document.documentElement.classList.remove('lang-cs','lang-en','lang-de');
        document.documentElement.classList.add('lang-'+l);document.documentElement.lang=l;
        document.title=TITLES[l]||TITLES.cs;
        var meta=document.querySelector('meta[name="description"]');
        if(meta)meta.setAttribute('content',DESCRIPTIONS[l]||DESCRIPTIONS.cs);
        document.querySelectorAll('.langpill button').forEach(function(b){b.classList.toggle('on',b.dataset.lang===l);});
        try{localStorage.setItem('lang',l);}catch(e){} window.__lang=l;
      }
      document.querySelectorAll('.langpill button').forEach(function(b){b.addEventListener('click',function(){apply(b.dataset.lang);});});
      apply(window.__lang||'cs');
    })();
  </script>

  <!--
    Verzovaná URL (count.v4.js je neměnná) + SRI: kdyby někdo podvrhl gc.zgo.at,
    prohlížeč skript neprovede. Při přechodu na v5 je nutné přepočítat hash:
    openssl dgst -sha384 -binary count.v5.js | openssl base64 -A
  -->
  {GOATCOUNTER && (
    <script
      is:inline
      async
      src="https://gc.zgo.at/count.v4.js"
      integrity="sha384-nRw6qfbWyJha9LhsOtSb2YJDyZdKvvCFh0fJYlkquSFjUxp9FVNugbfy8q1jdxI+"
      crossorigin="anonymous"
      referrerpolicy="no-referrer-when-downgrade"
      data-goatcounter={GOATCOUNTER}
    ></script>
  )}
</body>
</html>
